# Agent core (beacon/) + demo target app.
# Install into a repo-root venv (separate from backend/.venv):
#   python3 -m venv .venv && .venv/bin/pip install -r requirements-agent.txt
langgraph>=0.2
langchain>=0.3
langchain-core>=0.3
langchain-google-genai>=2.0
langchain-openai>=0.2
langchain-anthropic>=0.2
google-genai>=1.0
drain3>=0.9.11
GitPython>=3.1
PyYAML>=6.0
python-dotenv>=1.0
fastapi>=0.110
uvicorn>=0.29
pytest>=8.0
